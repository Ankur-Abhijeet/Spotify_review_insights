import React from 'react';

const Segments = () => {
  return (
    <div className="segments-page">
      <h2>User Segments</h2>
      <div className="glass-card" style={{ marginTop: '2rem' }}>
        <p style={{ color: 'var(--text-muted)' }}>Comparison dashboard for user types (Power Users vs Casuals) will go here.</p>
      </div>
    </div>
  );
};

export default Segments;
